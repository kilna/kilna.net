{{/*
  MeCard template - generates contact information
  Accessible at /kilna.mecard
*/}}
{{- if eq .Kind "page" -}}
MECARD:N:{{ .Params.name }};ORG:{{ .Params.organization }};TEL:{{ .Params.phone }};EMAIL:{{ .Params.email }};URL:{{ .Params.url }};;
{{- end -}}
